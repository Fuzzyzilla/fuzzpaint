# Fuzzpaint Types

The simple types used by Fuzzpaint, both graphical and otherwise. The complex
collections and inter-object relationships needed to represent a full document
are not implemented in this crate. Instead, the atoms a document is made of are
represented here. See [fuzzpaint-doc](../fuzzpaint-doc/) for higher-level
arrangements of these types sufficient to represent a whole document.
