# Fuzzpaint Renderer

A (possibly headless) Vulkan renderer for Fuzzpaint, capable of taking a
document and creating a raster image from it.

## UI
Adds additional capabilities to the renderer, including `egui` rendering and
realtime interaction.
